<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie, input_html: { data: { controller: 'select2' }, class: 'form-control'} %>
  <%= f.submit class: "btn btn-primary" %>
<% end %>
